{{! expects Object {types: {"type": "translationKey"} } }}
<fieldset class="flexcol">
    <label>{{localize 'FABULA_ULTIMA.dialog.addItem.label'}}</label>
    <select name="type">
        {{selectOptions types localize=true}}
    </select>
</fieldset>